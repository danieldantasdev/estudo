import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.foodies1',
  appName: 'Foodies1',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
