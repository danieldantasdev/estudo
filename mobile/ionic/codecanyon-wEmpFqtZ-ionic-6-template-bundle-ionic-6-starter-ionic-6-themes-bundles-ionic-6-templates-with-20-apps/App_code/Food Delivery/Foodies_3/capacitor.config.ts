import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.foodies3',
  appName: 'Foodies3',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
