import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.foodies2',
  appName: 'Foodie2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
