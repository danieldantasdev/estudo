import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.taxi1',
  appName: 'Taxi 1',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
