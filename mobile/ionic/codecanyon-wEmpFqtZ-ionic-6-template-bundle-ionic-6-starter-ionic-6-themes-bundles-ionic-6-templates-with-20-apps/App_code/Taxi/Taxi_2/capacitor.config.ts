import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.taxi2',
  appName: 'Taxi 2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
