import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.date2',
  appName: 'Date2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
