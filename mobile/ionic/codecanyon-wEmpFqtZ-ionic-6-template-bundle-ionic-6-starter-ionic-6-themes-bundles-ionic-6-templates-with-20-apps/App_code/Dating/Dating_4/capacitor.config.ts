import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.date4',
  appName: 'Date4',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
