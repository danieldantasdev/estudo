import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.shopping1',
  appName: 'Shopping 1',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
