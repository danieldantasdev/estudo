import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.shopping2',
  appName: 'Shopping 2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
