import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.salon2',
  appName: 'Salon2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
