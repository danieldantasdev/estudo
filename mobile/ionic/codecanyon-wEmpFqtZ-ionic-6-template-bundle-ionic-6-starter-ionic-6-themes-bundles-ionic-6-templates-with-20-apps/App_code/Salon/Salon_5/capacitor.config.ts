import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.salon5',
  appName: 'Salon 5',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
