import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.salon4',
  appName: 'Salon 4',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
