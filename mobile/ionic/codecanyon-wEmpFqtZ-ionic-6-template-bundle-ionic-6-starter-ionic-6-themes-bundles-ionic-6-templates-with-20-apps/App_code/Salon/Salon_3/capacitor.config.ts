import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.initappz.salon3',
  appName: 'Salon 3',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
