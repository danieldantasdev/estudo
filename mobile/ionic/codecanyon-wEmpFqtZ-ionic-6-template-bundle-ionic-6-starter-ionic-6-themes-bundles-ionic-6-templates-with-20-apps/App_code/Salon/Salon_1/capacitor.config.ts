import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.salon.user.bunchdevelopers',
  appName: 'Salon Napita',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
