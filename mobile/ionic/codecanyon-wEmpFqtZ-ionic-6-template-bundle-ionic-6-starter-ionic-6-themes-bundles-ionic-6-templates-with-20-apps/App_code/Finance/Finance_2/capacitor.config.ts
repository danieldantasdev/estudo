import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.bunchdevelopers.finance.two',
  appName: 'Finance 2',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
