import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.bunchdevelopers.finance.three',
  appName: 'Finance 3',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
